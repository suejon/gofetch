export type SiteConfig = typeof siteConfig;

export const siteConfig = {
  nav: [
    {
      name: 'Home',
      path: '/',
    },
    {
      name: 'Articles',
      path: '/articles',
    },
  ]
}
